require "spec_helper"

RSpec.describe Texton do
  it "has a version number" do
    expect(Texton::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
